package com.mjbre.model

object shorthands {
  type teamName = String
  type score = Int
  type totalPoints = Int
  type ranking = Int
  type teamTuple = ((teamName, totalPoints), (teamName, totalPoints))
}
